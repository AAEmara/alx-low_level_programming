#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Formats an output with printf function with a trailing newline.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
